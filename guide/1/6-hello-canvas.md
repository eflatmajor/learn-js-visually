---
{
  "outline": "deep"
}
---

# Chapter #6 - Hello, Canvas

:::tip TODO
- Briefly mention p5.js and that they don't need to know what it is or understand it at this point, all they need to know is it will make learning JS a bit easier.
- Introduce our first "hooks" - `setup()` and `draw()`
  - Teach how "hooks" are just functions that we define that will be called by some other code at certain points in time
  - Teach how `setup()` is ran once for initialisation
  - Teach how `draw()` is called for you 60 times per second
- Use `text()` to finally do our version of Hello World - render "Hello World" to the canvas
:::
