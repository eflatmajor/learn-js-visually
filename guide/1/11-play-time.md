# Chapter #11 - Play Time

:::tip TODO
- Have the learner try using `random()` in different places, like with stroke/fill colours of circles
- Change circle diameter based on `mouseX` or `mouseY`
- Have the user use what they've learned to create an array of shape objects with randomly chosen shapes like `square()`, `triangle()` etc.
- Mess around with the number of objects in the array?
- When creating the shape object array, make them all have randomised properties
- Can you use your knowledge of `width`/`height` and the division operator to create a grid of shapes?
- Make a "constellation of stars" - lots of very small circles scattered at random positions across the canvas. No interaction, just a static field of dots. It's simple but produces something genuinely beautiful, and it's achievable with just a `for` loop, `random()`, and `circle()`
:::
