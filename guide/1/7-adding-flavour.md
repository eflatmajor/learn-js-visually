# Chapter #7 - Adding Flavour

:::tip TODO
- Add `mouseX`/`mouseY` - interactivity: make the text follow the cursor
- Point out how the text is being duplicated all across the canvas, and introduce `background()` to prevent that by clearing the canvas every frame
- Introduce `width`/`height` and explain that they are dynamic too, like the mouse variables, and will update if the canvas is resized
- Introduce second operator - division (`/`)
- Use `width / 2` and `height / 2` to center our hello world text. Ask them if they can tell what the problem is (spoiler: it is not centered) -
- Introduce `textAlign()` and `CENTER` so that our text can be properly centered
:::
