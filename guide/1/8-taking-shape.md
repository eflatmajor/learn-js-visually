# Chapter #8 - Taking Shape

:::tip TODO
- Introduce our first shape (`circle()`)
  - Get learner to play with the diameter / position
- Introduce `random()` - randomise position, size/diameter etc.
- Teach `color()` and `colorMode()`
- Using colours (`stroke()` / `fill()`). Add an embedded colour picker and an info box about the colour formats that these functions support (perhaps link to another page that is in the "reference" section, not the "guide" section, that covers all of that colour stuff). Let the user pick their favourite colour with the picker and then get them to use it in the code
- Introduce `rect()` and `triangle`
:::
