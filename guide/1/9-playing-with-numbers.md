# Chapter #9 - Playing With Numbers

:::tip TODO
- Teach the "false path" - multiple variables for diameter/position/colour etc. of multiple circles
  - E.g. `circle1X, circle1Y, circle1Diameter, circle1FillColour` etc. repeated a few times for every circle
- Introduce (POJO) objects. Explain that they can group related data, show dot and bracket notation, show adding/removing properties (maybe?)
- The Big Pay-off -
  - Introduce arrays
  - Teach that JS arrays can hold *any* combination of datatypes
  - Show how we can group related objects in an array
  - Refactor circle variables into an array of objects
- Show a `for` loop - **the payoff moment after the "false path"** - user can now draw any number of circles, let's say 100, in 3 lines of code, whereas before we needed a `circle()` for every single circle, so 100 circles was 100 lines of code
:::

<!---
:::tip TODO
- Different ways of using `random()` (default behaviour of `0-1`, user-specified ranges, passing in arrays)
- Using `map()` to map the mouse position to different parameters e.g. (0% width is RBG 0,0,0 and 100% width is RGB 255,0,0)
- Teach `min()` and `max()`
- Making use of `constrain()` (perhaps to keep a smaller circle within a bigger circle) and mention how this function is just a shorthand for `return max(low, min(high, value))`
- Try combining `map()` AND `constrain()` together somehow
- Explain how the 3 rounding functions round numbers (`floor / round / ceil`)
- Show combining `floor()` with `random()` to simulate a dice roll
- Add FPS counter that uses `round()`
- Find an example for `ceil()`
:::
-->
