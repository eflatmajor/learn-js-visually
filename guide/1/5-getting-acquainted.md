# Chapter #5 - Getting Acquainted (with JavaScript)

:::tip TODO
- Introduce `let` and `function` keywords
- Teach basic variables and data-types (numbers, strings (no `const`/`var` for now) and maybe `null` (`undefined` later, or?)))
- Introduce first operator (assignment - `=`)
- Introduce semi-colons (maybe mention statements (what about expressions?) - or is it too early (probably)?)
- Introduce functions
  - What they do/why they're useful
  - How they are defined & how to name them
  - How to call them
  - How to pass data in and access it within the function (arguments/parameters)
  - How to get data back out again (`return` keyword) + how to store the result in a variable
- Introduce comments, and comment etiquette (don't rewrite the code in English (the WHAT), instead comment on the WHY, or sometimes the HOW)
- Introduce Ghetto Debugging (`console.*` methods)
:::
